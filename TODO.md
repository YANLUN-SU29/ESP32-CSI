# 開發路線圖 (TODO)

## ✅ 階段一：標準化資料收集 (已完成)
- [x] 確認物理配置：N300 路由器與 ESP32 距離固定 (約 1.5m)，Channel 6 / 20MHz。
- [x] 錄製 `Empty_10m` (空蕩房間)：純環境底噪，1 筆 10 分鐘。
- [x] 錄製 `Walk_LoS` (視距線走動)：人員於 LoS 來回走動，3 筆各約 2 分鐘。
- [x] 錄製 `Walk_NLoS` (非視距線走動)：人員於 NLoS 走動，3 筆各約 2 分鐘。
- [x] 錄製 `SitStand_1m` (起立坐下)：1 公尺距離內重複起立坐下，3 筆。
- [x] 錄製 `Breath_Center` (正面呼吸)：人員靜坐於 LoS 中央正常呼吸，3 筆。
- [x] 錄製 `Breath_Side` (側面呼吸)：人員靜坐於側邊正常呼吸，3 筆。
- [x] 錄製 `Apnea_Center` (閉氣對照)：人員靜坐閉氣作為對照組，3 筆。

## ✅ 階段二：資料驗證與訊號處理 (已完成)
- [x] 確認 CSV 格式正確 (53 欄: Timestamp + Sub_0~Sub_51)。
- [x] 驗證採樣率穩定 (~60.9 FPS)。
- [x] 實作 PCA 多載波融合 (52 條 → PC1 超級波形)。
- [x] 實作多層訊號清洗管線 (Rolling MAD → Detrend → Butterworth BP)。
- [x] 驗證呼吸偵測：Welch PSD 可抓到 0.15~0.4 Hz 峰值。
- [x] 驗證心跳偵測：0.8~2.0 Hz 帶通濾波可觀察到微弱訊號。
- [x] 優化 analyzer_gui.py：改用實際 Timestamp 換算 fps（取代固定假設值），並於 PCA 融合前剔除全零的 null/guard 子載波。
- [x] 優化 monitor.py：Timestamp 改為微秒精度（減少重複時間戳），並新增即時訊號斷點偵測與警告。
- [x] **修正 PCA 融合順序**：原本「先融合再濾波」完全無判別力（呼吸 3.48 vs 空房間 3.60），
      改為「先帶通濾波再融合」後才分離（65.90 vs 3.45）。
- [x] **建立客觀判定指標**：以最強子載波突出度 + 載波共識度取代肉眼判讀，
      經 20 筆呼吸 / 5 筆閉氣 / 5 筆空房間校準，排除 P0 後正確率 96%、零誤報。
- [x] 優化 analyzer_gui UI：新增結果面板（推估頻率/突出度/共識度/判定）、PSD 圖表、
      matplotlib 工具列、載入進度條與背景執行緒、長檔案繪圖抽樣。

> ⚠️ 先前「驗證呼吸偵測：Welch PSD 可抓到 0.15~0.4 Hz 峰值」的結論並不成立——
> 空房間資料同樣會在該頻段產生峰值。有了 EMPTY/HOLD 對照組後才真正完成驗證。

## ✅ 韌體與擷取佈局修正（已完成）
- [x] 韌體改以 `CSI_V2,<len>,<rssi>,<sig_mode>,<csi...>` 輸出中繼資料，並加入長度校驗。
- [x] 韌體關閉 Wi-Fi modem sleep (`WiFi.setSleep(false)`)。
- [x] 以一次性診斷腳本實測確認 384 bytes = LLTF / HT-LTF / STBC-HT-LTF2 三個 64 條區塊
      （量完即刪，未保留於專案中）。
- [x] **修正子載波擷取範圍**：舊版寫死 `amplitudes[5:57]`，存了 9 條全零保護頻帶又切斷兩端，
      實際可用僅 43 條；改為依實測佈局取 LLTF 54 條 + HT-LTF 56 條 = 110 條（可用條數 +156%）。
      捨棄與 HT-LTF 相關係數 0.902 的冗餘 STBC 區塊，並剔除各區塊的 DC 離群值。

## ✅ 序列埠負載與穩定性（已完成，2026-09-02 實測）
- [x] 關閉 `stbc_htltf2_en`：`len` 384 → 256 bytes，間隔中位數 8.00ms → 5.00ms，
      **平均 fps 59.1 → 85.0（+44%）**。證實瓶頸在序列埠吞吐量而非 MAC 層
      （先前判斷為 TCP delayed ACK 有誤）。
- [x] `client.connect()` 加 500ms 逾時：每分鐘斷點率（>0.1s）2.68 → 0.52，改善 5 倍。
- [x] `get_com_port()` 只挑有 VID/PID 的 USB 裝置，不再誤鎖 COM1 並拋出 `OSError 22`。
- [x] CSV 改為序列埠連上後才建立，不再留下只有標題列的空檔。
- [x] 序列埠斷線偵測：`in_waiting` 包在 try 內，連續 500 筆失敗即結束並說明原因。
- [x] 新增 ICMP ping 觸發模式（`USE_ICMP_PING`，預設 0 未啟用，程式碼備用）。

## ✅ 收發鏈收尾（已完成，待實機驗證）
- [x] **納入 CSI 相位**：一併記錄原始 `atan2(Q,I)`（`RECORD_PHASE`），CSV 111 → 221 欄。
      存原始值不做 CFO/SFO 校正——校正屬分析階段，採集階段不應破壞原始資料。
      `analyzer_gui.py` 依 `_A`/`_P` 後綴只取振幅欄分析（已回歸測試三種歷史格式）。
- [x] **丟失率變成可量測**：CSI 回呼改為只把資料丟進 FreeRTOS 佇列（非阻塞），
      由 `loop()` 排空輸出；`delay(10)` 改為 `millis()` 排程以免阻塞排空。
      行格式升級為 `CSI_V3,<seq>,<len>,<rssi>,<sig_mode>,<dropped>,<csi...>`。
      PC 端據序號跳號算出傳輸遺失量、據 `dropped` 得知 ESP32 端丟棄量，
      結束時輸出完整丟失率分析。
      （原本 `Serial.println` 在回呼內阻塞會使 Wi-Fi 驅動停止送入事件，
      該類損失連計數器都測不到。）

## ✅ 110 條格式驗證與門檻重新校準（已完成，2026-09-08 實測 20 筆）
- [x] **確認新格式下呼吸偵測正常**：命中 11/12、誤報 0/8、正確率 95.0%、AUC 0.958。
      推估呼吸率 12.4~13.1 次/分（各位置組內標準差 ±0.1~0.5），對照組則為 10.9~17.1 ±1.3~3.2。
- [x] **Welch 窗長由 60s 改為 30s**：60s 在約 100 秒的錄製下只有 1~2 段平均，
      空房間曾出現突出度 24.0 的假峰值；30s 有約 4 段平均後降至 5.5，AUC 由 0.917 升至 0.958。
      實測對照組「時長 vs 突出度」相關 r = −0.818，證實假峰值來自平均不足。
- [x] **門檻重新校準**：突出度 ≥ 20 且共識 ≥ 70%（舊，43 條 / 60s 窗長）
      → **突出度 ≥ 5.5 且共識 ≥ 90%**（新，110 條 / 30s 窗長）。安全裕度 2.7。
      註：突出度尺度隨窗長改變，兩者必須一起調整。
- [x] **HT-LTF 擴充確認有效**：單用 HT-LTF 的 AUC 0.948 反而高於單用 LLTF 的 0.917；
      12 筆呼吸資料中最強子載波 LLTF 佔 6 筆、HT-LTF 佔 6 筆，兩區塊皆有貢獻。
- [x] **共識度確認為必要條件**：空房間 EMPTY03 突出度 5.5 但共識僅 80%，
      單看突出度無法零誤報，須靠共識度剔除。
- [x] **GEO_P0 不再是盲區**：本批 P0 反而是最強組（突出度 20.4，舊資料為 4.5 近似空房間）。
- [x] 新增 `batch_analyze.py`：可重現的批次分析與門檻校準工具，直接沿用 analyzer_gui 的
      清理與峰值函式以確保一致，支援快取、頻段切換、相位分析。

## 🔴 相位實驗：目前不可用
- [ ] **相位以現行處理方式無判別力**：AUC 僅 0.375（低於 0.5，即反向）。
      所有組別（含空房間）的推估頻率都收斂到 9.1~9.9 次/分、共識度多為 100%，
      集中在頻段下緣 0.15 Hz 附近——研判為殘留低頻漂移洩漏，
      而非呼吸訊號。現行處理為 unwrap + 逐封包跨子載波線性去趨勢，顯然不足。
      需改進校正方法（或確認 ESP32 相位雜訊是否本質上過大）後再評估。

## 🟠 待處理（依實測發現排序）
- [ ] 評估二進位輸出取代 ASCII 十進位：每個 int8 現以 2~5 字元傳送，約 3~4 倍膨脹。
      改二進位/Base64 預估可將 fps 再推至 120~160，但需重寫封包同框與解析。
- [ ] **記錄新資料集定義**：`P0/P05/P10/P15` 各代表什麼配置、`HOLD` 的實驗條件，目前文件完全沒有記載。
- [x] ~~實機驗證新版 `monitor.py`~~ 已完成：微秒時間戳生效，重複時間戳由 10.7% 降至 0.00%。
- [x] ~~釐清 fps 卡在 59 的原因~~ 已釐清：**瓶頸在序列埠吞吐量,不是 MAC 層**。
      原先依「每 78ms 週期有 47ms 閒置」推論為路由器 TCP delayed ACK，此判斷有誤。
      證據：封包長度縮 1/3（384→256 bytes）後，間隔中位數精確由 8.00ms 降至 5.00ms、
      fps 由 59.1 升至 85.0。此等比關係說明叢發內的間隔即序列埠傳一行的時間，
      序列埠來不及排空時 ESP32 端的 CSI 事件即被丟棄。
- [ ] **加入 ESP32 端硬體時間戳**：PC 端時間戳被 USB 1ms 幀率量化（間隔全落在整數毫秒上），
      無法解析真實的 CSI 擷取時刻，導致 `WiFi.setSleep(false)` 是否有效無從驗證。
- [ ] 校準大動作 / 心跳頻段的判定門檻（目前缺對照組資料，GUI 已標示未校準）。
      註：2026-09-02 的房間測試在心跳頻段出現突出度 90.8、推估 69.1 次/分，
      落在合理靜息心率範圍，但該頻段零校準且環境漂移能量會漏入 0.8~2.0 Hz，不可視為偵測到。
- [ ] `firmware.ino` 未過濾來源 MAC，任何裝置的封包都會觸發 CSI，鄰居流量會混入。

## 🟡 階段三：進階分析與 AI 建模 (進行中)
- [ ] 整合 CWT (連續小波變換) 生成時頻譜圖 (Scalogram)。
- [ ] 評估 EMD / VMD 自適應分解法取代固定頻段濾波。
- [ ] 建立 CNN/LSTM 模型進行動作分類 (Walking / Sitting / Breathing / Empty)。
- [ ] 評估 Transfer Learning 可行性 (跨場景泛化)。

## 🔵 階段四：系統整合 (規劃中)
- [ ] 邊緣運算：將推論模型部署至 ESP32 或樹莓派。
- [ ] 即時警報機制：偵測到跌倒或異常靜止時發送通知。
- [ ] 多設備協同感測：多個 ESP32 覆蓋更大空間。
